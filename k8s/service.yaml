apiVersion: v1
kind: Service
metadata:
  name: opsmind-app-service
  namespace: opsmind
spec:
  type: NodePort
  selector:
    app: opsmind-app
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080