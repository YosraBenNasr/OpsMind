apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: opsmind-servicemonitor
  namespace: opsmind
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: opsmind-app
  endpoints:
    - port: http-metrics
      path: /metrics
      interval: 15s