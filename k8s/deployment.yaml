apiVersion: apps/v1
kind: Deployment
metadata:
  name: opsmind-app
  namespace: opsmind
  labels:
    app: opsmind-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: opsmind-app
  template:
    metadata:
      labels:
        app: opsmind-app
    spec:
      containers:
      - name: opsmind-app
        image: ghcr.io/yosrabennasr/opsmind:d3cc524
        ports:
        - containerPort: 8000
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "256Mi" # Limite stricte pour déclencher l'OOMKilled
            cpu: "500m"